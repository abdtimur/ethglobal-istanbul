# Exclude everything
*

# Include workspace members
!proxy
## Include root certs from client_wasm
!client/ca-certificates.crt
## Include source, but not its WASM artifacts
!source
source/pkg
!verifier

# Include Cargo files
!/Cargo.lock
!/Cargo.toml

# Include notary public key and verifier secret key
!fixture/debug-notary.pub
!fixture/debug-verifier.key
